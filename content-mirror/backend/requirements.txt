# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0           # async driver (FastAPI)
psycopg2-binary==2.9.9    # sync driver (Celery worker)
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Config
pydantic==2.8.2
pydantic-settings==2.3.4

# Cache / Queue
redis==5.0.7
celery==5.4.0
kombu==5.3.4

# Storage
boto3==1.34.144          # S3 / R2
aiofiles==23.2.1

# PDF export
weasyprint==62.3
jinja2==3.1.4            # HTML templates for PDF

# Rate limiting
slowapi==0.1.9

# HTTP
httpx==0.27.0

# AI dependencies (used by worker)
anthropic==0.32.0
openai==1.37.1           # Whisper API fallback

# Monitoring
sentry-sdk[fastapi]==2.10.0

# Dev / Test
pytest==8.3.1
pytest-asyncio==0.23.7
httpx==0.27.0            # test client
aiosqlite==0.20.0        # in-memory SQLite for tests
